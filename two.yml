---
- name: Step Two 
  hosts: management
  become: yes
  vars_files:
    - step2_var.yml

  tasks:
  - name: write management node hostname in /etc/hostname
    ansible.builtin.lineinfile:
      path: /etc/hostname 
      line: "{{ clustername }}1"
